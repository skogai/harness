# commands

Installable source category for Claude slash-command definition files.

Current contents are examples only. Installing this category copies the files to
`.claude/commands/`; it does not create a command runtime beyond the copied
Markdown definitions.

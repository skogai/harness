# Політика вентиля стану passing

Функція може перейти з `passes: false` у `passes: true` лише якщо:

- очікуваний робочий процес був виконаний
- свідчення успіху зафіксовані
- у протестованому шляху немає блокуючих помилок
- реалізація не залишає застосунок у зламаному або неоднозначному стані
